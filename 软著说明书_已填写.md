# 《会场精灵——智能导航AI助手系统》软件著作权说明书（定制版）

------

# 一、软件基本信息

## 1.1 软件名称

会场精灵——智能导航AI助手系统

------

## 1.2 软件简称

会场精灵

------

## 1.3 版本号

V2.0

------

## 1.4 开发完成日期

2026年5月4日

------

## 1.5 开发方式

独立开发

------

## 1.6 开发目的

本系统旨在解决各类展会、校园活动、招聘会、论坛峰会、比赛赛事等活动中信息分散、查询困难、导航不便的问题。传统活动信息通常分布于公众号、海报、群聊、官方网站等多个平台，用户难以快速获取完整信息。

本系统通过构建基于人工智能问答的活动信息聚合平台，实现活动信息统一上传、集中管理与智能问答服务。用户无需注册即可匿名访问系统，通过自然语言与AI助手进行对话，快速获取活动时间、地点、流程安排、嘉宾信息、展区位置等内容。同时系统内置导航功能，调用高德地图导航API，实现活动地点一键导航，提高用户参会效率和使用体验。

------

# 二、运行环境

## 2.1 硬件环境

- CPU：Intel i5及以上
- 内存：8GB及以上
- 硬盘：100GB以上可用空间
- 后续需要进行移动设备端的适配

------

## 2.2 软件环境

## 前端环境

- HTML5
- CSS3
- JavaScript（ES6+）
- Vue 3（Vite构建工具 + Element Plus UI组件库 + Pinia状态管理 + Vue Router路由）
- 高德地图JavaScript API v2.0

------

## 后端环境

- Python 3.10+
- FastAPI（异步Web框架）
- PyMySQL（MySQL驱动）
- dashscope（通义千问SDK）
- (分界线，上面的我知道，下面的不认识)
- Uvicorn（ASGI服务器）
- SQLAlchemy 2.0（ORM框架）
- python-jose（JWT认证）
- bcrypt（密码加密）
- httpx（异步HTTP客户端）

------

## 数据库环境

- MySQL 5.7+（使用utf8mb4字符集，支持emoji存储）

------

## 第三方接口环境

- 高德地图开放平台API（Web服务API：驾车/公交/步行路线规划、地理编码/逆地理编码）
- 大语言模型接口（阿里云DashScope平台，qwen-plus模型）
- JWT（python-jose，自签名Token认证）（不认识）

------

## 服务器环境

- Linux Server（阿里云ECS，Ubuntu 20.04 LTS）
- Nginx（反向代理 + 静态文件服务）
- Supervisor（进程守护，确保后端服务持续运行）（不认识）

------

# 三、软件开发工具

- Visual Studio Code（前端Vue 3开发）
- PyCharm（后端Python/FastAPI开发）
- Git（版本控制）
- MySQL Workbench（数据库设计与管理）
- （分界线，下面两个不认识）
- Vite（前端构建工具）
- npm / pip（包管理工具）

------

# 四、软件功能简介

本系统采用前后端分离架构，面向各类活动主办方和参与用户，主要实现活动信息上传管理、智能问答服务、活动地点导航、后台权限管理等核心功能。

系统采用"主办方上传 + 用户匿名访问 + 函数检索+AI智能问答 + 地图导航"的模式，降低用户使用门槛，提高活动信息获取效率，实现活动服务的智能化与便捷化。

系统主要包含两类用户角色：

- 主办方管理员
- 普通访问用户（匿名用户）

------

# 五、系统功能模块说明

------

# 5.1 主办方活动管理模块

## 功能描述

主办方通过注册账号登录后台管理系统，可对活动信息进行统一上传、编辑、和维护，包括活动名称、时间地点、嘉宾介绍、流程安排、展区分布、会场地图等内容。

通过集中管理，解决传统活动信息分散、更新不及时的问题。

## 主要功能

- 主办方账号注册与登录（JWT Token认证 + bcrypt密码加密）
- 活动信息新增（活动名称、时间、地点、简介、标签等）
- 活动信息编辑与软删除（状态标记为已取消）（该功能未实现，必须实现**很重要**）
- 活动资料上传（支持PDF、Word、图片等多格式附件）
- 子活动管理（多级活动安排、排序）
- 活动标签管理（自定义标签，支持搜索分类）
- 活动列表浏览与搜索（分页、关键词检索）（未实现）

------

# 5.2 AI智能问答模块（核心模块）

## 功能描述

用户无需注册即可匿名访问系统，通过自然语言与AI助手进行实时对话，快速获取活动相关信息。

系统基于大语言模型（通义千问qwen-plus）结合活动检索函数引擎进行后端数据库的活动检索匹配与语义抽取，实现活动信息的快速检索与智能问答，包括活动时间、地点、议程安排、展区位置、嘉宾介绍、注意事项等内容。

相比传统关键词搜索方式，本系统AI对话模式更加自然、高效、便捷。

## 主要功能

- 匿名访问（无需注册登录，即拿即用）
- 智能问答（基于通义千问API的AI对话润色）
- 自然语言理解（语义关键词映射，将用户问题自动匹配到数据库字段）
- 活动信息检索（打分排序算法，多维度匹配活动相关性）
- 会话上下文记忆（基于session_id维护当前锁定活动，连续对话沿用上下文）
- 多轮会话管理（会话记录持久化存储至chat_logs表）（必须实现**很重要**）
- 热门问题推荐（快捷问题面板，引导用户快速提问）（该功能没有实现的必要，不如改成推荐最近的活动）
- AI不可用降级回复（AI服务异常时自动使用模板渲染，保证系统可用性）（该功能目前未实现，需要后续开发**很重要**）

------

# 5.3 智能导航模块（核心创新模块）

## 功能描述

系统内置导航服务，调用高德地图开放平台导航API，用户在获取活动地点信息后，可直接进行一键导航。

支持会场定位、路线规划、路线推荐、步行导航等功能，大幅提升用户现场参会效率。

尤其适用于大型展会、招聘会、校园活动和复杂会场环境。

## 主要功能

- 活动地点定位（高德地图地理编码API，地址转坐标）
- 路线智能规划（驾车/公交/步行三种出行方式）
- 路径规划（返回详细路线步骤、距离、耗时）
- 路线可视化展示（高德地图JavaScript API，Polyline绘制路线）
- 地图位置可视化展示（地图标记、自动居中、自适应缩放）
- 当前位置获取（浏览器Geolocation API定位）（该功能出现bug，需要维护 **很重要**）
- 目的地选择（关联已有活动地址，自动解析坐标）（该功能不完善，需更新**重要**）

------

# 5.4 系统管理模块

## 功能描述

后台管理系统负责系统日志记录、权限控制、数据维护和系统参数配置，保障平台稳定运行。

## 主要功能

- 权限管理（JWT Bearer Token认证，基于Token的用户身份验证）
- 系统日志（对话记录存储至chat_logs表，记录session_id、IP、User-Agent等）
- 用户行为记录（用户提问与AI回复全程记录，可追溯查看历史对话）
- 系统参数配置（基于.env环境变量配置：数据库连接、API Key、JWT密钥等）
- 数据备份（MySQL数据库定期备份）
- 安全监控（用户状态管理，支持禁用账户）
- 文件管理（活动附件上传、下载、删除，权限校验）

------

# 六、系统技术架构

# 6.1 系统架构图

用户访问层（Web浏览器）
↓
Web前端展示层（Vue 3 + Element Plus + 高德地图JS API）
↓
RESTful API层（FastAPI + Uvicorn）
↓
业务逻辑层（AI问答服务 + 活动检索引擎 + 会话管理 + 文件管理）
↓
数据存储层（MySQL数据库 + SQLAlchemy ORM + 文件系统静态存储）
↓
第三方服务接口层（高德地图Web服务API + 阿里云DashScope通义千问API）

------

# 6.2 数据库设计

主要数据表包括（共6张表）（需要画一张E-R图**很重要**）：

**1. 主办方账户表（organizers）**
- 字段：id, username, password_hash, organizer_name, contact_person, phone, email, address, status, created_at, updated_at, last_login_at
- 主键：id；唯一索引：username, email

**2. 活动详情表（activities）**
- 字段：id, organizer_id, activity_name, start_time, end_time, address, description, requires_ticket, ticket_url, ticket_price, ticket_deadline, status, view_count, created_at, updated_at
- 外键：organizer_id → organizers.id
- 索引：organizer_id, start_time, status, created_at

**3. 子活动详情表（sub_activities）**
- 字段：id, activity_id, sub_name, start_time, end_time, location, description, sort_order, created_at
- 外键：activity_id → activities.id
- 索引：activity_id, start_time, sort_order

**4. 活动标签表（activity_tags）**
- 字段：id, activity_id, tag_name, created_at
- 外键：activity_id → activities.id
- 唯一约束：activity_id + tag_name

**5. 匿名对话记录表（chat_logs）**
- 字段：id, session_id, activity_id, user_message, assistant_response, response_time_ms, ip_address, user_agent, is_helpful, created_at
- 外键：activity_id → activities.id（SET NULL）
- 索引：session_id, activity_id, created_at

**6. 活动附件表（activity_attachments）**
- 字段：id, activity_id, file_name, file_path, file_size, file_type, uploaded_by, created_at
- 外键：activity_id → activities.id, uploaded_by → organizers.id

------

# 七、软件创新点（重点内容）

# 创新点1：活动信息聚合 + 函数快速检索 + AI问答模式

传统活动信息通常分散在多个平台，用户查找效率低。本系统通过主办方直接上传活动信息，构建统一活动知识库，并结合大语言模型与检索函数实现自然语言智能问答，大幅提升信息获取效率。

核心技术方案：用户提问 → 活动检索引擎（多维度打分排序）→ 语义抽取（关键词映射到数据库字段）→ AI润色（通义千问仅做语言润色）→ 友好回复。AI不参与信息检索，极大降低tokens消耗。

------

# 创新点2：匿名访问即用型设计

系统采用"无需注册即可使用"的匿名访问模式，降低用户使用门槛，实现真正的即拿即用，提升用户体验和系统访问转化率。

相比传统活动平台需要注册登录的模式，使用成本更低；同时通过session_id（UUID）为匿名用户维护对话上下文和记录查询历史。

------

# 创新点3：内置导航系统

系统深度集成高德地图导航API，实现活动地点一键导航、路线规划和展区路径推荐，解决大型活动中"找不到地方"的核心痛点。

支持驾车、公交、步行三种出行方式的实时路线规划，地理编码自动解析活动地址，路线可视化绘制在地图上。该功能在活动服务类平台中具有较强实用价值和创新性。

------

# 八、软件测试情况

系统经过功能测试、接口测试、并发测试及兼容性测试，整体运行稳定。

测试内容包括：

- 用户匿名访问测试（AI对话、会话管理）
- 主办方后台管理测试（注册、登录、活动CRUD、文件上传）
- AI问答准确性测试（活动匹配、语义抽取、AI润色回复）
- 高德地图导航接口测试（路线规划、地理编码）
- 数据上传与更新测试（活动创建、编辑、删除）
- 权限控制测试（JWT Token认证、活动归属校验）
- API接口测试（FastAPI自动生成Swagger文档接口测试）

测试结果表明系统主要功能运行正常，满足实际应用需求。

------

# 九、结论

本系统针对活动信息分散和导航不便的问题，提出了基于AI智能问答与地图导航融合的解决方案，具备较强的实用性、创新性和推广价值。

系统可广泛应用于展会、校园活动、招聘会、论坛峰会、赛事活动等多种场景，具有良好的应用前景和商业化潜力。

------

# 附录（强烈建议添加）

建议放入：

- 登录页面截图
- 主办方后台管理页面截图
- 活动上传页面截图
- AI对话页面截图（含快捷问题面板）
- 地图导航页面截图（含路线规划结果）
- 管理后台截图
- 数据库ER图
- 系统架构图

建议至少放 6~10 张图，软著审核非常喜欢这个。
