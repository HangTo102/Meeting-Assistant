# 会场精灵v2 - 技术路线

## 项目定位

中小型会场信息助手应用，用于比赛展示。核心目标：**完整实现预期功能**，部署用免费方案。

---

## 技术栈

### 前端
| 技术 | 说明 |
|------|------|
| React 18 + TypeScript | 核心框架 |
| Vite | 构建工具（快速开发） |
| React Router v6 | 路由管理 |
| Zustand | 状态管理（轻量） |
| Ant Design | UI组件库（中文文档） |
| Tailwind CSS | 样式系统 |
| 高德地图 JS API | 地图集成 |

### 后端
| 技术 | 说明 |
|------|------|
| FastAPI | Python异步框架 |
| SQLAlchemy + Alembic | ORM + 数据库迁移 |
| Pydantic | 数据验证 |
| PostgreSQL / SQLite | 数据库 |

### 外部服务
| 服务 | 用途 |
|------|------|
| 通义千问 (DashScope) | AI对话 |
| 高德地图 API | 地理位置服务 |

---

## 数据库设计（6张核心表）

```
organizers        # 主办方（活动管理）
    ↓
activities        # 活动
    ↓
sub_activities    # 子活动
activity_tags     # 活动标签

conversations     # 匿名会话
    ↓
messages          # 对话消息
```

---

## 功能模块

### 前端页面
1. **首页** - 活动推荐、快速搜索
2. **活动列表** - 筛选、排序、无限滚动
3. **活动详情** - 信息展示、地图定位、子活动时间线
4. **智能对话** - AI助手聊天、文件上传
5. **主办方后台** - 活动上传管理

### 后端API
- `/api/activities` - 活动CRUD、地理位置搜索
- `/api/chat` - AI对话、会话管理
- `/api/auth` - 主办方认证
- `/api/upload` - 文件上传

---

## 开发计划（4阶段，约8周）

### 阶段一：基础框架（1-2周）
- [ ] React项目初始化 + TypeScript
- [ ] 路由配置
- [ ] 基础布局组件（Header、Sidebar、Layout）
- [ ] Zustand状态管理搭建
- [ ] 后端FastAPI骨架 + 数据库模型

### 阶段二：核心功能（2-3周）
- [ ] 活动列表页 + 详情页
- [ ] 高德地图集成
- [ ] AI对话基础功能
- [ ] 主办方登录/注册
- [ ] 活动上传功能

### 阶段三：完善功能（2-3周）
- [ ] 活动筛选搜索
- [ ] 对话历史管理
- [ ] 文件上传（活动封面）
- [ ] 子活动管理
- [ ] 移动端适配

### 阶段四：优化上线（1-2周）
- [ ] 性能优化
- [ ] 错误处理完善
- [ ] UI细节打磨
- [ ] 部署上线

---

## 部署方案（开发期免费）

| 服务 | 平台 | 成本 |
|------|------|------|
| 前端 | Vercel | $0（免费） |
| 后端 | Railway | $0（500小时/月） |
| 数据库 | Railway PostgreSQL | $0（1GB存储） |

**上线后再考虑**：如果国内用户多，迁移到阿里云（约¥200/月）

---

## 关键技术要点

### 1. 地图集成
```typescript
// 高德地图初始化
const map = new AMap.Map('container', {
  zoom: 11,
  center: [longitude, latitude]
});
```

### 2. AI对话
- 调用通义千问API
- 维护会话上下文
- 匿名session追踪

### 3. 数据交互
- Axios调用后端API
- React Query缓存优化
- WebSocket实时消息（可选）

---

## 项目结构

```
venue-assistant-v2/
├── frontend/           # React前端
│   ├── src/
│   │   ├── api/       # API客户端
│   │   ├── components/# UI组件
│   │   ├── pages/     # 页面
│   │   ├── stores/    # Zustand状态
│   │   └── hooks/     # 自定义Hook
│   └── package.json
│
├── backend/            # FastAPI后端
│   ├── src/
│   │   ├── api/       # 路由
│   │   ├── core/      # 业务逻辑
│   │   ├── database/  # 模型/CRUD
│   │   ├── services/  # 外部服务
│   │   └── main.py    # 入口
│   └── requirements.txt
│
├── docker-compose.yml  # 本地开发
└── 技术路线.md          # 本文档
```

---

## 开发环境启动

```bash
# 后端
cd backend
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8000

# 前端
cd frontend
npm install
npm run dev
```

---

## 下一步行动

1. **确认技术栈** → 本文档已确定
2. **初始化项目** → 创建前后端目录结构
3. **开始阶段一** → 搭建基础框架

---

*最后更新：2026-03-30*